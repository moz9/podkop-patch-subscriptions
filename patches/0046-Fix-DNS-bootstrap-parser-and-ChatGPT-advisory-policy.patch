From be48691a3fa8a6857f8ab00a3dba7d77b0e9e735 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Fri, 14 Aug 2026 10:21:15 +0700
Subject: [PATCH] Fix DNS bootstrap parsing and ChatGPT advisory policy


diff --git a/podkop/files/usr/bin/podkop-dns-optimizer b/podkop/files/usr/bin/podkop-dns-optimizer
index 5272924..f7bb02c 100755
--- a/podkop/files/usr/bin/podkop-dns-optimizer
+++ b/podkop/files/usr/bin/podkop-dns-optimizer
@@ -1,5 +1,5 @@
 #!/bin/ash

-VERSION="20260814-dns-optimizer-v16"
+VERSION="20260814-dns-optimizer-v17"
 GOOGLE_PLAY_GUARD_CAPABILITY="google_play_dns_transport_guard_v1"
 CHATGPT_GUARD_CAPABILITY="chatgpt_dns_transport_guard_v1"
@@ -520,9 +520,9 @@ base|google_downloads|dl.google.com
 base|google_downloads|redirector.gvt1.com
 base|chatgpt|chatgpt.com
-base|chatgpt|chat.openai.com
 base|chatgpt|api.openai.com
-base|chatgpt|auth.openai.com
-base|chatgpt|cdn.oaistatic.com
-base|chatgpt|files.oaiusercontent.com
+advisory|chatgpt|chat.openai.com
+advisory|chatgpt|auth.openai.com
+advisory|chatgpt|cdn.oaistatic.com
+advisory|chatgpt|files.oaiusercontent.com
 base|gaming|steamcdn-a.akamaihd.net
 base|gaming|media.steampowered.com
@@ -711,5 +711,5 @@ dns_output_has_usable_a() {

     addresses="$(awk '
-        $0 !~ /^;/ && $(NF - 1) == "A" && $NF ~ /^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$/ { print $NF }
+        NF >= 2 && $0 !~ /^;/ && $(NF - 1) == "A" && $NF ~ /^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$/ { print $NF }
     ')"
     [ -n "$addresses" ] || return 1
@@ -813,5 +813,5 @@ probe_configured_pair() {
     local slot="$1"
     local pair protocol dns_server bootstrap host endpoint_ip tls_hostname path domain elapsed successes total
-    local play_dns_available chatgpt_dns_available error
+    local play_dns_available chatgpt_dns_available chatgpt_api_dns_available error

     pair="$(read_configured_pair "$slot" || true)"
@@ -837,8 +837,9 @@ probe_configured_pair() {

     successes=0
-    total=4
+    total=5
     play_dns_available=0
     chatgpt_dns_available=0
-    for domain in example.com github.com play.googleapis.com chatgpt.com; do
+    chatgpt_api_dns_available=0
+    for domain in example.com github.com play.googleapis.com chatgpt.com api.openai.com; do
         elapsed="$(run_dns_query "$protocol" "$endpoint_ip" "$tls_hostname" "$path" "$domain" NOERROR 2> /dev/null || true)"
         if [ -n "$elapsed" ]; then
@@ -846,7 +847,11 @@ probe_configured_pair() {
             [ "$domain" != "play.googleapis.com" ] || play_dns_available=1
             [ "$domain" != "chatgpt.com" ] || chatgpt_dns_available=1
+            [ "$domain" != "api.openai.com" ] || chatgpt_api_dns_available=1
         fi
     done
-    if [ "$successes" -ge 3 ] && [ "$play_dns_available" -eq 1 ] && [ "$chatgpt_dns_available" -eq 1 ]; then
+    if [ "$successes" -ge 4 ] \
+        && [ "$play_dns_available" -eq 1 ] \
+        && [ "$chatgpt_dns_available" -eq 1 ] \
+        && [ "$chatgpt_api_dns_available" -eq 1 ]; then
         jq -cn --arg slot "$slot" --arg protocol "$protocol" --argjson successes "$successes" --argjson total "$total" \
             '{success:true,slot:$slot,protocol:$protocol,successes:$successes,total:$total}'
@@ -858,4 +863,6 @@ probe_configured_pair() {
     elif [ "$chatgpt_dns_available" -ne 1 ]; then
         error="chatgpt_dns_unavailable"
+    elif [ "$chatgpt_api_dns_available" -ne 1 ]; then
+        error="chatgpt_api_dns_unavailable"
     fi
     jq -cn --arg slot "$slot" --arg protocol "$protocol" --arg error "$error" --argjson successes "$successes" --argjson total "$total" \
@@ -994,4 +1001,6 @@ benchmark_main_candidate() {
     local compatibility_failures compatibility_unstable universal_eligible community_probe_total community_probe_passed community_probe_stable
     local community_score community_selected community_tested community_passed community_stable selected_community_json primary_eligible
+    local advisory_counts advisory_total advisory_passed advisory_stable advisory_score advisory_failures advisory_unstable
+    local required_success_count required_total_queries

     host="$(candidate_host "$value")"
@@ -1016,4 +1025,11 @@ benchmark_main_candidate() {
     community_probe_stable="${community_counts%%|*}"
     community_probe_total="${community_counts#*|}"
+    advisory_counts="$(calculate_probe_scope_counts advisory "$successful_file" "$probes_file")"
+    advisory_passed="${advisory_counts%%|*}"
+    advisory_counts="${advisory_counts#*|}"
+    advisory_stable="${advisory_counts%%|*}"
+    advisory_total="${advisory_counts#*|}"
+    required_success_count=0
+    required_total_queries=$(((compatibility_total + community_probe_total) * 2))
     community_selected="$(wc -l < "$selected_file" | tr -d ' ')"
     selected_community_json="$(jq -Rsc 'split("\n") | map(select(length > 0))' "$selected_file")"
@@ -1026,7 +1042,9 @@ benchmark_main_candidate() {
             --argjson compatibilityTotal "$compatibility_total" --argjson selectedCommunityServices "$selected_community_json" \
             --argjson communitySelected "$community_selected" --argjson communityProbeTotal "$community_probe_total" \
+            --argjson advisoryPassed "$advisory_passed" --argjson advisoryStable "$advisory_stable" --argjson advisoryTotal "$advisory_total" \
+            --argjson requiredSuccessCount "$required_success_count" --argjson requiredTotalQueries "$required_total_queries" \
             --argjson primaryEligible "$primary_eligible" \
             --slurpfile communityResults "$community_results_file" \
-            '{id:$id,provider:$provider,dnsServer:$dnsServer,profile:$profile,protocol:$protocol,primaryEligible:$primaryEligible,successCount:0,totalQueries:$totalQueries,successRate:0,medianMs:0,p90Ms:0,jitterMs:0,nxdomainOk:false,compatibilityPassed:0,compatibilityStable:0,compatibilityTotal:$compatibilityTotal,compatibilityScore:0,compatibilityFailures:[],compatibilityUnstable:[],selectedCommunityServices:$selectedCommunityServices,communitySelected:$communitySelected,communityTested:0,communityPassed:0,communityStable:0,communityProbePassed:0,communityProbeStable:0,communityProbeTotal:$communityProbeTotal,communityScore:0,communityResults:$communityResults,universalEligible:false,reliable:false,score:-1000000,error:$error}'
+            '{id:$id,provider:$provider,dnsServer:$dnsServer,profile:$profile,protocol:$protocol,primaryEligible:$primaryEligible,successCount:0,totalQueries:$totalQueries,successRate:0,requiredSuccessCount:$requiredSuccessCount,requiredTotalQueries:$requiredTotalQueries,medianMs:0,p90Ms:0,jitterMs:0,nxdomainOk:false,compatibilityPassed:0,compatibilityStable:0,compatibilityTotal:$compatibilityTotal,compatibilityScore:0,compatibilityFailures:[],compatibilityUnstable:[],advisoryPassed:$advisoryPassed,advisoryStable:$advisoryStable,advisoryTotal:$advisoryTotal,advisoryScore:0,advisoryFailures:[],advisoryUnstable:[],selectedCommunityServices:$selectedCommunityServices,communitySelected:$communitySelected,communityTested:0,communityPassed:0,communityStable:0,communityProbePassed:0,communityProbeStable:0,communityProbeTotal:$communityProbeTotal,communityScore:0,communityResults:$communityResults,universalEligible:false,reliable:false,score:-1000000,error:$error}'
         rm -f "$probes_file" "$selected_file" "$successful_file" "$community_results_file"
         return 0
@@ -1080,6 +1098,24 @@ benchmark_main_candidate() {
     done

+    base_counts="$(calculate_probe_scope_counts base "$successful_file" "$probes_file")"
+    compatibility_passed="${base_counts%%|*}"
+    base_counts="${base_counts#*|}"
+    compatibility_stable="${base_counts%%|*}"
+    compatibility_total="${base_counts#*|}"
+    community_counts="$(calculate_probe_scope_counts community "$successful_file" "$probes_file")"
+    community_probe_passed="${community_counts%%|*}"
+    community_counts="${community_counts#*|}"
+    community_probe_stable="${community_counts%%|*}"
+    community_probe_total="${community_counts#*|}"
+    advisory_counts="$(calculate_probe_scope_counts advisory "$successful_file" "$probes_file")"
+    advisory_passed="${advisory_counts%%|*}"
+    advisory_counts="${advisory_counts#*|}"
+    advisory_stable="${advisory_counts%%|*}"
+    advisory_total="${advisory_counts#*|}"
+    required_success_count=$((compatibility_passed + compatibility_stable + community_probe_passed + community_probe_stable))
+    required_total_queries=$(((compatibility_total + community_probe_total) * 2))
+
     nxdomain_ok=false
-    if [ "$success" -ge $((total - 1)) ]; then
+    if [ "$required_success_count" -ge $((required_total_queries - 1)) ]; then
         if is_ipv4 "$host"; then
             output="$(run_dns_query "$protocol" "$endpoint_ip" "" "$path" "podkop-dns-test.invalid" NXDOMAIN 2> /dev/null || true)"
@@ -1097,14 +1133,4 @@ benchmark_main_candidate() {
     p90="${stats#*|}"
     success_rate=$((success * 100 / total))
-    base_counts="$(calculate_probe_scope_counts base "$successful_file" "$probes_file")"
-    compatibility_passed="${base_counts%%|*}"
-    base_counts="${base_counts#*|}"
-    compatibility_stable="${base_counts%%|*}"
-    compatibility_total="${base_counts#*|}"
-    community_counts="$(calculate_probe_scope_counts community "$successful_file" "$probes_file")"
-    community_probe_passed="${community_counts%%|*}"
-    community_counts="${community_counts#*|}"
-    community_probe_stable="${community_counts%%|*}"
-    community_probe_total="${community_counts#*|}"
     compatibility_score=$(((compatibility_passed * 70 + compatibility_stable * 30) / compatibility_total))
     compatibility_failures="$(awk -F '|' '
@@ -1116,4 +1142,17 @@ benchmark_main_candidate() {
         $1 == "base" && successes[$1 "|" $2 "|" $3] == 1 { print $3 }
     ' "$successful_file" "$probes_file" | jq -Rsc 'split("\n") | map(select(length > 0))')"
+    advisory_failures="$(awk -F '|' '
+        NR == FNR { successes[$0]++; next }
+        $1 == "advisory" && successes[$1 "|" $2 "|" $3] == 0 { print $3 }
+    ' "$successful_file" "$probes_file" | jq -Rsc 'split("\n") | map(select(length > 0))')"
+    advisory_unstable="$(awk -F '|' '
+        NR == FNR { successes[$0]++; next }
+        $1 == "advisory" && successes[$1 "|" $2 "|" $3] == 1 { print $3 }
+    ' "$successful_file" "$probes_file" | jq -Rsc 'split("\n") | map(select(length > 0))')"
+    if [ "$advisory_total" -gt 0 ]; then
+        advisory_score=$(((advisory_passed * 70 + advisory_stable * 30) / advisory_total))
+    else
+        advisory_score=100
+    fi
     build_community_results "$successful_file" "$probes_file" "$selected_file" "$community_results_file" true
     community_tested="$(jq -s 'map(select(.tested == true)) | length' "$community_results_file")"
@@ -1129,5 +1168,5 @@ benchmark_main_candidate() {
     universal_eligible=false
     error=""
-    if [ "$success" -ge $((total - 1)) ] \
+    if [ "$required_success_count" -ge $((required_total_queries - 1)) ] \
         && [ "$compatibility_passed" -eq "$compatibility_total" ] \
         && [ "$community_probe_passed" -eq "$community_probe_total" ] \
@@ -1137,5 +1176,5 @@ benchmark_main_candidate() {
             universal_eligible=true
         fi
-    elif [ "$success" -ge $((total - 1)) ] && [ "$nxdomain_ok" != true ]; then
+    elif [ "$required_success_count" -ge $((required_total_queries - 1)) ] && [ "$nxdomain_ok" != true ]; then
         score=$((score - 500000))
         error="nxdomain_check_failed"
@@ -1154,4 +1193,5 @@ benchmark_main_candidate() {
         --arg bootstrapProfile "$bootstrap_profile" --arg error "$error" \
         --argjson successCount "$success" --argjson totalQueries "$total" --argjson successRate "$success_rate" \
+        --argjson requiredSuccessCount "$required_success_count" --argjson requiredTotalQueries "$required_total_queries" \
         --argjson medianMs "$median" --argjson p90Ms "$p90" --argjson jitterMs "$jitter" \
         --argjson bootstrapSuccessRate "$bootstrap_success_rate" --argjson bootstrapMedianMs "$bootstrap_median" \
@@ -1161,4 +1201,6 @@ benchmark_main_candidate() {
         --argjson compatibilityStable "$compatibility_stable" --argjson compatibilityScore "$compatibility_score" \
         --argjson compatibilityFailures "$compatibility_failures" --argjson compatibilityUnstable "$compatibility_unstable" \
+        --argjson advisoryPassed "$advisory_passed" --argjson advisoryStable "$advisory_stable" --argjson advisoryTotal "$advisory_total" \
+        --argjson advisoryScore "$advisory_score" --argjson advisoryFailures "$advisory_failures" --argjson advisoryUnstable "$advisory_unstable" \
         --argjson selectedCommunityServices "$selected_community_json" --argjson communitySelected "$community_selected" \
         --argjson communityTested "$community_tested" --argjson communityPassed "$community_passed" --argjson communityStable "$community_stable" \
@@ -1167,5 +1209,5 @@ benchmark_main_candidate() {
         --argjson nxdomainOk "$nxdomain_ok" --argjson primaryEligible "$primary_eligible" --argjson universalEligible "$universal_eligible" --argjson reliable "$reliable" --argjson score "$score" \
         --slurpfile communityResults "$community_results_file" \
-        '{id:$id,provider:$provider,dnsServer:$dnsServer,profile:$profile,protocol:$protocol,primaryEligible:$primaryEligible,endpointIp:$endpointIp,bootstrapDnsServer:$bootstrapDnsServer,bootstrapProvider:$bootstrapProvider,bootstrapProfile:$bootstrapProfile,bootstrapSuccessRate:$bootstrapSuccessRate,bootstrapMedianMs:$bootstrapMedianMs,bootstrapP90Ms:$bootstrapP90Ms,bootstrapJitterMs:$bootstrapJitterMs,bootstrapUniversalEligible:$bootstrapUniversalEligible,successCount:$successCount,totalQueries:$totalQueries,successRate:$successRate,medianMs:$medianMs,p90Ms:$p90Ms,jitterMs:$jitterMs,nxdomainOk:$nxdomainOk,compatibilityPassed:$compatibilityPassed,compatibilityStable:$compatibilityStable,compatibilityTotal:$compatibilityTotal,compatibilityScore:$compatibilityScore,compatibilityFailures:$compatibilityFailures,compatibilityUnstable:$compatibilityUnstable,selectedCommunityServices:$selectedCommunityServices,communitySelected:$communitySelected,communityTested:$communityTested,communityPassed:$communityPassed,communityStable:$communityStable,communityProbePassed:$communityProbePassed,communityProbeStable:$communityProbeStable,communityProbeTotal:$communityProbeTotal,communityScore:$communityScore,communityResults:$communityResults,universalEligible:$universalEligible,reliable:$reliable,score:$score,error:(if $error == "" then null else $error end)}'
+        '{id:$id,provider:$provider,dnsServer:$dnsServer,profile:$profile,protocol:$protocol,primaryEligible:$primaryEligible,endpointIp:$endpointIp,bootstrapDnsServer:$bootstrapDnsServer,bootstrapProvider:$bootstrapProvider,bootstrapProfile:$bootstrapProfile,bootstrapSuccessRate:$bootstrapSuccessRate,bootstrapMedianMs:$bootstrapMedianMs,bootstrapP90Ms:$bootstrapP90Ms,bootstrapJitterMs:$bootstrapJitterMs,bootstrapUniversalEligible:$bootstrapUniversalEligible,successCount:$successCount,totalQueries:$totalQueries,successRate:$successRate,requiredSuccessCount:$requiredSuccessCount,requiredTotalQueries:$requiredTotalQueries,medianMs:$medianMs,p90Ms:$p90Ms,jitterMs:$jitterMs,nxdomainOk:$nxdomainOk,compatibilityPassed:$compatibilityPassed,compatibilityStable:$compatibilityStable,compatibilityTotal:$compatibilityTotal,compatibilityScore:$compatibilityScore,compatibilityFailures:$compatibilityFailures,compatibilityUnstable:$compatibilityUnstable,advisoryPassed:$advisoryPassed,advisoryStable:$advisoryStable,advisoryTotal:$advisoryTotal,advisoryScore:$advisoryScore,advisoryFailures:$advisoryFailures,advisoryUnstable:$advisoryUnstable,selectedCommunityServices:$selectedCommunityServices,communitySelected:$communitySelected,communityTested:$communityTested,communityPassed:$communityPassed,communityStable:$communityStable,communityProbePassed:$communityProbePassed,communityProbeStable:$communityProbeStable,communityProbeTotal:$communityProbeTotal,communityScore:$communityScore,communityResults:$communityResults,universalEligible:$universalEligible,reliable:$reliable,score:$score,error:(if $error == "" then null else $error end)}'
     rm -f "$probes_file" "$selected_file" "$successful_file" "$community_results_file"
 }
@@ -1470,5 +1512,7 @@ restart_podkop() {
 validate_service_compatibility() {
     local server="$1"
-    local probes selected successful scope service domain round output total passed success_count
+    local probes selected successful scope service domain round output total passed stable success_count
+    local base_counts community_counts base_passed base_stable base_total
+    local community_passed community_stable community_total

     probes="$(mktemp)"
@@ -1485,12 +1529,18 @@ validate_service_compatibility() {
     done

-    total="$(wc -l < "$probes" | tr -d ' ')"
-    passed="$(awk -F '|' '
-        NR == FNR { successes[$0]++; next }
-        successes[$1 "|" $2 "|" $3] >= 1 { passed++ }
-        END { print passed + 0 }
-    ' "$successful" "$probes")"
-    success_count="$(wc -l < "$successful" | tr -d ' ')"
-    success_count=$((success_count - 1))
+    base_counts="$(calculate_probe_scope_counts base "$successful" "$probes")"
+    base_passed="${base_counts%%|*}"
+    base_counts="${base_counts#*|}"
+    base_stable="${base_counts%%|*}"
+    base_total="${base_counts#*|}"
+    community_counts="$(calculate_probe_scope_counts community "$successful" "$probes")"
+    community_passed="${community_counts%%|*}"
+    community_counts="${community_counts#*|}"
+    community_stable="${community_counts%%|*}"
+    community_total="${community_counts#*|}"
+    total=$((base_total + community_total))
+    passed=$((base_passed + community_passed))
+    stable=$((base_stable + community_stable))
+    success_count=$((passed + stable))
     rm -f "$probes" "$selected" "$successful"
     [ "$passed" -eq "$total" ] && [ "$success_count" -ge $((total * 2 - 1)) ]
