diff --git a/luci-app-podkop/htdocs/luci-static/resources/view/podkop/section.js b/luci-app-podkop/htdocs/luci-static/resources/view/podkop/section.js
index 806eb74..34a9ac4 100644
--- a/luci-app-podkop/htdocs/luci-static/resources/view/podkop/section.js
+++ b/luci-app-podkop/htdocs/luci-static/resources/view/podkop/section.js
@@ -6,6 +6,14 @@
 "require view.podkop.main as main";

 function createSectionContent(section) {
+  function hasListValue(value) {
+    if (Array.isArray(value)) {
+      return value.some((item) => item != null && String(item).trim() !== "");
+    }
+
+    return value != null && String(value).trim() !== "";
+  }
+
   let o = section.option(
     form.ListValue,
     "connection_type",
@@ -30,6 +38,7 @@ function createSectionContent(section) {
   o.value("outbound", _("Outbound Config"));
   o.default = "url";
   o.depends("connection_type", "proxy");
+  const proxyConfigTypeOption = o;

   o = section.option(
     form.TextValue,
@@ -67,7 +76,8 @@ function createSectionContent(section) {
     _("HTTP/HTTPS subscription links with proxy URLs")
   );
   o.depends("proxy_config_type", "subscription_urltest");
-  o.rmempty = false;
+  o.rmempty = true;
+  const subscriptionUrlOption = o;
   o.validate = function (section_id, value) {
     if (!value || value.length === 0) {
       return true;
@@ -152,7 +162,39 @@ function createSectionContent(section) {
   );
   o.depends("proxy_config_type", "urltest");
   o.depends("proxy_config_type", "subscription_urltest");
-  o.rmempty = false;
+  o.rmempty = true;
+  const baseUrltestProxyLinksParse = o.parse;
+  o.parse = function (section_id) {
+    const proxyConfigType =
+      proxyConfigTypeOption.formvalue(section_id) ||
+      proxyConfigTypeOption.cfgvalue(section_id);
+    const proxyLinks = this.formvalue(section_id);
+    const subscriptionUrls = subscriptionUrlOption.formvalue(section_id);
+
+    if (this.isActive(section_id) && !hasListValue(proxyLinks)) {
+      if (proxyConfigType === "urltest") {
+        const title = this.stripTags(this.title).trim();
+        return Promise.reject(
+          new TypeError(
+            _('Option "%s" must not be empty.').format(title || this.option)
+          )
+        );
+      }
+
+      if (
+        proxyConfigType === "subscription_urltest" &&
+        !hasListValue(subscriptionUrls)
+      ) {
+        return Promise.reject(
+          new TypeError(
+            "\u0417\u0430\u043f\u043e\u043b\u043d\u0438\u0442\u0435 URL \u043f\u043e\u0434\u043f\u0438\u0441\u043e\u043a \u0438\u043b\u0438 \u041f\u0440\u043e\u043a\u0441\u0438-\u0441\u0441\u044b\u043b\u043a\u0438 \u0434\u043b\u044f \u0440\u0435\u0436\u0438\u043c\u0430 \u041c\u0438\u043a\u0441."
+          )
+        );
+      }
+    }
+
+    return baseUrltestProxyLinksParse.call(this, section_id);
+  };
   o.validate = function (section_id, value) {
     // Optional
     if (!value || value.length === 0) {
